<h1>Edit advocate</h1>
 
<%= form_with(model: @advocate, local: true) do |form| %>
 
  <% if @advocate.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@advocate.errors.count, "error") %> prohibited
        this advocate from being saved:
      </h2>
      <ul>
        <% @advocate.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>


  <p>
    <%= form.submit %>
  </p>
 
<% end %>
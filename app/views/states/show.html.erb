<p>
  <strong>name:</strong>
  <%= @state.name %>
</p>
<table>
    <th>client_name</th>
    <th>state</th>
    <th>status</th>
    <th>date</th> 
  </tr>
 


  <strong>cases:</strong>

   <% @state.cases.each do |advocate_case| %>
    <tr>
     
      <td><%= advocate_case.client_name %></td>
      <td><%= advocate_case.state.name %></td>
      <td><%= advocate_case.status %></td>
      <td><%= advocate_case.date %></td>
 </tr>
  <% end %>
</table>>

<%= link_to 'Edit', edit_state_path(@state) %> |
<%= link_to 'Back', states_path %>